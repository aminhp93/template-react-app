export type TFile = {
  id?: number,
  filePreview?: string,
  fileKey: string,
  fileType: string,
  width?: number | null,
  height?: number | null,
  index?: number,
  name?: string,
  size?: number | string,
}
